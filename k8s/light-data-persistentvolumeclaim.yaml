apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: light-data
  name: light-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
